<Window x:Class="KeyFixQR.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="KeyFix QR — Settings" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize" ShowInTaskbar="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="0" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid Background="Transparent">
        <Border Margin="16" CornerRadius="16" Background="{DynamicResource BgBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <Grid Margin="20,14,20,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,12">
                    <TextBlock Text="KeyFix QR" FontSize="17" FontWeight="Bold"/>
                    <Button HorizontalAlignment="Right" Content="✕" Style="{StaticResource IconButton}" Click="CloseBtn_Click"/>
                </Grid>

                <ScrollViewer Grid.Row="1" MaxHeight="520" VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock x:Name="kbTitle" Style="{StaticResource SectionTitle}"/>
                                <CheckBox x:Name="kbEnable"/>
                                <TextBlock x:Name="fixShortcutLabel" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="fixShortcutBox" IsReadOnly="True" FontSize="13" FontWeight="SemiBold"
                                         HorizontalAlignment="Stretch" Cursor="Hand"
                                         GotFocus="ShortcutBox_GotFocus" PreviewKeyDown="ShortcutBox_PreviewKeyDown"/>
                                <ComboBox x:Name="directionCombo" Margin="0,10,0,4" SelectionChanged="DirectionCombo_Changed">
                                    <ComboBoxItem x:Name="dirItemAuto"/>
                                    <ComboBoxItem x:Name="dirItemEn2Fa"/>
                                    <ComboBoxItem x:Name="dirItemFa2En"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock x:Name="qrTitle" Style="{StaticResource SectionTitle}"/>
                                <CheckBox x:Name="qrEnable"/>
                                <TextBlock x:Name="qrShortcutLabel" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="qrShortcutBox" IsReadOnly="True" FontSize="13" FontWeight="SemiBold"
                                         HorizontalAlignment="Stretch" Cursor="Hand"
                                         GotFocus="ShortcutBox_GotFocus" PreviewKeyDown="ShortcutBox_PreviewKeyDown"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock x:Name="generalTitle" Style="{StaticResource SectionTitle}"/>
                                <CheckBox x:Name="startupCheck"/>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="themeLabel" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox Grid.Column="2" x:Name="themeCombo" MinWidth="150">
                                        <ComboBoxItem x:Name="themeItemLight"/>
                                        <ComboBoxItem x:Name="themeItemDark"/>
                                        <ComboBoxItem x:Name="themeItemAuto"/>
                                    </ComboBox>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="languageLabel" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox Grid.Column="2" x:Name="languageCombo" MinWidth="150" SelectionChanged="LanguageCombo_Changed">
                                        <ComboBoxItem x:Name="langItemFa"/>
                                        <ComboBoxItem x:Name="langItemEn"/>
                                    </ComboBox>
                                </Grid>
                                <TextBlock Text=" " Height="6"/>
                                <TextBlock x:Name="privacyNote" Style="{StaticResource HintText}"/>
                            </StackPanel>
                        </Border>

                        <TextBlock x:Name="hintText" Style="{StaticResource HintText}" Margin="4,0,4,10"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="statusText" Foreground="#DC2626" VerticalAlignment="Center" TextWrapping="Wrap"/>
                            <Button Grid.Column="1" x:Name="cancelBtn" Style="{StaticResource GhostButton}" Click="CancelBtn_Click" Margin="8,0,0,0"/>
                            <Button Grid.Column="2" x:Name="saveBtn" Style="{StaticResource AccentButton}" Click="SaveBtn_Click" Margin="8,0,0,0"/>
                        </Grid>

                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
