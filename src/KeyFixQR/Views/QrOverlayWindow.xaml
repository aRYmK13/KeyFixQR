<Window x:Class="KeyFixQR.Views.QrOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="KeyFix QR" Width="290" SizeToContent="Height"
        WindowStartupLocation="Manual" ShowInTaskbar="False" ShowActivated="False"
        Topmost="True" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="CanResizeWithGrip"
        PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="12" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid x:Name="rootGrid" Background="Transparent" MouseLeftButtonDown="RootGrid_MouseLeftButtonDown">
        <Border Margin="14" CornerRadius="14" Background="{DynamicResource CardBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="26" ShadowDepth="2" Opacity="0.35"/>
            </Border.Effect>
            <StackPanel Margin="14,10,14,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="QR Code" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" x:Name="closeBtn" Content="✕" Style="{StaticResource IconButton}" Click="CloseBtn_Click" ToolTip="ESC"/>
                </Grid>
                <Image x:Name="qrImage" Height="230" Stretch="Uniform" Margin="4,8,4,4"/>
                <TextBlock x:Name="payloadPreview" Style="{StaticResource HintText}" MaxHeight="56"
                           TextTrimming="CharacterEllipsis" Margin="2,2,2,10"/>
                <Button x:Name="copyBtn" Style="{StaticResource AccentButton}" Click="CopyBtn_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
